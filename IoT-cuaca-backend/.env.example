# MongoDB connection string
MONGO_URI=mongodb://localhost:27017
# Database name
DB_NAME=weatherdb
# Secret used for signing JWT tokens (choose a strong random value)
JWT_SECRET=change-me-to-a-strong-secret
# API key expected from IoT devices when POSTing to /api/sensor
IOT_API_KEY=your_iot_api_key_here
# (Optional) allowed frontend origin for CORS in production (dev uses *)
# FRONTEND_URL=http://localhost:5173
